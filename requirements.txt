# --- API frameworks ---
fastapi==0.115.*
uvicorn[standard]==0.30.*
flask==3.0.*                 # ingestion webhook microservice (see app_flask_ingest.py)

# --- Auth / security ---
python-jose[cryptography]==3.3.*
passlib==1.7.*
bcrypt==4.0.1                 # pinned: passlib 1.7.x is incompatible with bcrypt>=4.1
python-multipart==0.0.*        # form/file uploads
email-validator==2.*

# --- Data / ORM ---
sqlalchemy==2.0.*
pydantic==2.*
pydantic-settings==2.*

# --- RAG / vector store ---
chromadb==0.5.*
langchain-text-splitters==0.2.*
scikit-learn==1.5.*
numpy>=1.26

# --- Document loaders ---
pypdf==4.*

# --- Optional: real LLM generation (set LLM_PROVIDER=openai) ---
openai==1.*

# --- Testing ---
pytest==8.*
httpx==0.27.*
